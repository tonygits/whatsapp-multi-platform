export type DeviceKey = {
    id: number,
    userId: string,
    numberHash: string,
    apiKeyId: string,
    deactivatedAt?: string,
    encryptedToken: string,
    createdAt: string,
    updatedAt: string
}
