export type DeviceKey = {
    id: number,
    userId: string,
    deviceHash: string,
    apiKeyId: string,
    deactivatedAt?: string,
    encryptedToken: string,
    createdAt: string,
    updatedAt: string
}
