export type DeviceKey = {
    id: number,
    userId: string,
    deviceId: number,
    apiKeyId: string,
    deactivatedAt?: string,
    encryptedToken: string,
    createdAt: string,
    updatedAt: string
}
