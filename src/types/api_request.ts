export type ApiRequest = {
    id: string,
    userId: string,
    userAgent: string,
    ipAddress: string,
    deviceHash: string,
    endpoint: string,
    method: string,
    createdAt: string,
}
