export type Subscription = {
    id: string,
    code: string,
    email: string,
    customerId: string,
    planCode: string,
    numberHash: string,
    status: string,
    nextBillingDate?: string,
    createdAt: string,
    updatedAt: string,
}
