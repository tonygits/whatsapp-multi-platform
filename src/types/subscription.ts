export type Subscription = {
    id: string,
    code: string,
    email: string,
    customerId: string,
    planCode: string,
    status: string,
    nextBillingDate?: string,
    createdAt: string,
    updatedAt: string,
}
