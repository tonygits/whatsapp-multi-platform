export type Customer = {
    id: string,
    authorizationCode: string,
    customerId: string,
    firstName?: string,
    lastName?: string,
    email: string,
    phone?: string,
    userId: string,
    deviceHash: string,
    createdAt: string,
    updatedAt: string,
}
